import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      <Button className="cursor-pointer bg-red-50">Button</Button>
      <h1>Home</h1>
    </div>
  );
};

export default Home;
